var assert = require('chai').assert;

describe('Google Books CLI Test Cases', () => {
    describe('API Key', () => {
        it('should be passed as a string into the http request', () => {
            assert('key goes here', 'string')
        })
    })
})