import { searchBooks } from "./utils/search.js";

// const books = await searchBooks("faces")

// books.data.items.forEach((item, i) => {
//     const {id} = item.id
//     const {title, authors, publisher } = item.volumeInfo
//     // console.log(`
//     //     title: ${title}
//     //     author: ${authors || "no author"}
//     //     publisher: ${publisher}
//     // `)
//     console.log(item)
// });